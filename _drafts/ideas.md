# Parked blog ideas

- [2026-04-18] AI plans hide value behind jargon: the roadmapper named a phase "Employer Representation Helper" when the real story was "your Swedish tax forms print the wrong name in the employer field — it's only working today because your patient happens to be a minor." I only caught it when I had to ask "what is phase 8 even for?"
- [2026-04-18] Edit-frequency is the question that exposes over-engineered UX: a "logical by topic" 5-section Settings dialog survived discuss-phase by sounding clean, and only collapsed to 2 sections when I asked "how often is Skatt & bank actually edited?" — turns out almost never after hire, which means fewer cards = faster scan when one thing changes. The PM instinct that fired was treating "i dont understand anything" as a real signal instead of a request to re-explain.
- [2026-04-19] Deleting a deferred-idea note from my own planning doc taught me something about signal-to-noise: I asked Claude mid-discuss-phase whether pay was 26th-to-25th, accepted "keep calendar month for v1.0.1", and then told Claude to *delete* the deferred entry it had helpfully written about the alternative. The lesson: not every "what about…" deserves to live in your artifacts forever — some questions get asked, answered, and should disappear, because the deferred-ideas section is supposed to be load-bearing for future phases, not a graveyard of every conversational tangent. Carrying everything forward is its own form of clutter.
- [2026-04-19] Your compiler is lying to you: ORM-generated types pass tsc/build while the live DB is unmigrated — a [BLOCKING] push task is the cheap fix.
- [2026-04-20] Adjacency bugs — when two tool outputs in the same LLM context disagree and that contradiction is what saves you: resuming Phase 9, GSD's `init` step said a SUMMARY file existed while its `phase-plan-index` step said `has_summary: false` for the same plan, because the tool's filename regex only matched one of two legitimate naming conventions (`09-01-SUMMARY.md` short form vs `09-01-schema-and-whitelists-SUMMARY.md` long form). Nothing I did manually caught it — the workflow fed both contradictory JSON blobs into Claude's context within a few turns of each other, and the self-contradiction was visible in the same message. Without that adjacency the executor would have duplicated 6 committed features. Practical patterns to bring into the draft: (1) run `/gsd-progress` before `/gsd-execute-phase` after any session gap — it reads ROADMAP.md checkboxes, a different source than the filename-regex tool; (2) treat git as source of truth, planning tools as projections — before trusting any "incomplete" signal, `git log --oneline --grep="{phase}-{plan}"` and stop if commits exist; (3) patch `phase-plan-index` to accept both SUMMARY filename conventions — 2-line regex fix that eliminates the whole class; (4) make the orchestrator show its work — adjacent contradictory tool outputs surface at review time instead of post-damage; (5) add a `gsd-health` pre-check that walks every phase and XORs (commits exist) against (long-form SUMMARY exists); (6) prefer `--interactive` mode on resumption so tool state is verified against reality at every task boundary. Meta-takeaway for the draft: the workflow should reconcile its own two sources before spawning agents — 3 lines of "for each plan in `incomplete`, assert no SUMMARY exists on disk under any pattern" would turn a silent false-positive into a loud warning.
- [2026-04-20] Agents short-circuit human-in-the-loop UAT: asked to "run the tests for me" on a conversational UAT designed for human eyes, I read JWT_SECRET from .env and forged guardian+assistant tokens instead of asking for passwords — the human-gated workflow became a fully automated one by accident, which raises the PM question of whether that is the win or the bug.
- [2026-05-01] Agentic handoff moment: Claude Dockerized the app, caught two real bugs mid-run (SPA catch-all swallowing API 404s; husky crashing runtime npm ci), then paused at a human-verify gate — a concrete picture of where AI coding hands back to a human and why.
